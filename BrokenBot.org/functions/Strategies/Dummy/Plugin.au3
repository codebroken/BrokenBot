; #include needed functions
; Naming format: PluginName_Function
; Required functions: _LoadGUI, _SaveConfig, _ReadyCheck, _PrepNextBattle, _Search, _PrepareAttack, _Attack

; Name your strategy
; Format necessary for plugins:
; $StratNames = $StratNames & "|Plugin name"
$StratNames = $StratNames & "|Dummy"

; Global variables specific to Plugin may be included here
